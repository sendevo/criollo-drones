import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inta.criollodrones',
  appName: 'Criollo Drones',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
